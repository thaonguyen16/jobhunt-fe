export type Static = {
    access: number;
    job: number;
    revenue: number;
}

export type TrafficChart = {
    date: string;
    number: number;
}

export type IndustryChart = {
    name: string;
    value: number;
    color: string;
}